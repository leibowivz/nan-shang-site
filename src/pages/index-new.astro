---
import Base from "../layouts/Base.astro";
---

<Base>
  <!-- Fluid Gradient Background (like Klyro) -->
  <div class="fluid-bg">
    <div class="gradient-orb gradient-orb-1"></div>
    <div class="gradient-orb gradient-orb-2"></div>
    <div class="gradient-orb gradient-orb-3"></div>
  </div>

  <!-- Noise Texture Overlay -->
  <div class="noise-overlay"></div>

  <!-- Navigation -->
  <nav class="nav-modern">
    <div class="nav-content">
      <a href="#" class="nav-brand">
        <span class="brand-bracket">[</span>
        <span class="brand-text">NS</span>
        <span class="brand-bracket">]</span>
      </a>
      <div class="nav-links">
        <a href="#work">Work</a>
        <a href="#research">Research</a>
        <a href="#contact" class="nav-cta">Contact</a>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero-modern">
    <div class="hero-content">
      <div class="hero-kicker">
        <span class="blink">▮</span> Chief Scientist & Founding Engineer
      </div>
      
      <h1 class="hero-title">
        <span class="title-line">I build systems</span>
        <span class="title-line title-gradient">that last.</span>
      </h1>

      <p class="hero-desc">
        Where data meets uncertainty. Where the real test is not performance in isolation,
        but <span class="highlight">behavior over time</span>. I design geospatial and AI
        systems that stay quiet, stable, and correct.
      </p>

      <div class="hero-meta">
        <div class="meta-item">
          <span class="meta-label">NAME</span>
          <span class="meta-value">Nan Shang, Ph.D.</span>
        </div>
        <div class="meta-divider"></div>
        <div class="meta-item">
          <span class="meta-label">ROLE</span>
          <span class="meta-value">Chief Scientist</span>
        </div>
        <div class="meta-divider"></div>
        <div class="meta-item">
          <span class="meta-label">ORG</span>
          <span class="meta-value">Drone Institute</span>
        </div>
      </div>

      <div class="hero-cta">
        <a href="#work" class="btn-primary">
          <span>Explore Work</span>
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
            <path d="M3 8h10M8 3l5 5-5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          </svg>
        </a>
        <a href="https://scholar.google.com/citations?user=TcDAyj0AAAAJ&hl=en" 
           target="_blank" class="btn-secondary">Google Scholar</a>
        <a href="https://github.com/leibowivz" 
           target="_blank" class="btn-secondary">GitHub</a>
      </div>
    </div>

    <!-- ASCII Art Visualization -->
    <div class="ascii-viz">
      <pre class="ascii-scene">
╔════════════════════════════════════╗
║  GEOSPATIAL SYSTEM ARCHITECTURE   ║
╠════════════════════════════════════╣
║                                    ║
║     ┌─────────┐    ┌──────────┐  ║
║     │ SENSORS │───▶│ PIPELINE │  ║
║     └─────────┘    └──────────┘  ║
║          │              │         ║
║          ▼              ▼         ║
║     ┌─────────┐    ┌──────────┐  ║
║     │  DATA   │───▶│   AI     │  ║
║     └─────────┘    └──────────┘  ║
║          │              │         ║
║          ▼              ▼         ║
║     ┌─────────┐    ┌──────────┐  ║
║     │   QC    │───▶│ DELIVERY │  ║
║     └─────────┘    └──────────┘  ║
║                                    ║
╚════════════════════════════════════╝
      </pre>
    </div>
  </section>

  <!-- Stats Bar -->
  <div class="stats-bar">
    <div class="stat">
      <span class="stat-value">10+</span>
      <span class="stat-label">Production Systems</span>
    </div>
    <div class="stat-divider"></div>
    <div class="stat">
      <span class="stat-value">24/7</span>
      <span class="stat-label">Uptime Monitor</span>
    </div>
    <div class="stat-divider"></div>
    <div class="stat">
      <span class="stat-value">99.9%</span>
      <span class="stat-label">Reliability</span>
    </div>
  </div>

  <main class="main-content">
    
    <!-- Philosophy Section -->
    <section class="section" id="philosophy">
      <div class="section-header">
        <span class="section-number">01</span>
        <h2 class="section-title">How I Build Systems</h2>
      </div>

      <div class="cards-grid">
        <div class="card-modern">
          <div class="card-icon">🎯</div>
          <h3 class="card-title">Production is the benchmark</h3>
          <p class="card-desc">
            A system is only meaningful if it continues to behave correctly after deployment.
            I design systems to fail visibly, support iteration, and remain interpretable.
          </p>
        </div>

        <div class="card-modern">
          <div class="card-icon">⚡</div>
          <h3 class="card-title">Constraints shape architecture</h3>
          <p class="card-desc">
            Latency, throughput, cost, and operational friction are architectural facts.
            I make trade-offs explicit so systems can be maintained over time.
          </p>
        </div>

        <div class="card-modern">
          <div class="card-icon">🔄</div>
          <h3 class="card-title">Deployment reveals assumptions</h3>
          <p class="card-desc">
            Research doesn't end at deployment. I design feedback loops that surface
            failure modes and allow systems to improve without eroding stability.
          </p>
        </div>

        <div class="card-modern">
          <div class="card-icon">📊</div>
          <h3 class="card-title">Data quality compounds</h3>
          <p class="card-desc">
            Long-term advantage comes from disciplined data practices. Ingestion,
            annotation, QC, monitoring, and rollback reinforce one another.
          </p>
        </div>
      </div>
    </section>

    <!-- Systems Section -->
    <section class="section" id="work">
      <div class="section-header">
        <span class="section-number">02</span>
        <h2 class="section-title">Systems I Build</h2>
      </div>

      <div class="systems-list">
        <div class="system-item">
          <div class="system-header">
            <div class="system-status">
              <span class="status-dot"></span>
              <span class="status-text">ACTIVE</span>
            </div>
            <span class="system-tag">PIPELINE</span>
          </div>
          <h3 class="system-name">End-to-End Geospatial Pipelines</h3>
          <p class="system-desc">
            Full lifecycle — ingestion, normalization, processing, verification, reporting,
            delivery. Repeatable, auditable, resilient under scale.
          </p>
          <div class="system-tech">
            <span class="tech-tag">GIS</span>
            <span class="tech-tag">PostgreSQL</span>
            <span class="tech-tag">Python</span>
            <span class="tech-tag">S3</span>
          </div>
        </div>

        <div class="system-item">
          <div class="system-header">
            <div class="system-status">
              <span class="status-dot"></span>
              <span class="status-text">ACTIVE</span>
            </div>
            <span class="system-tag">SENSING</span>
          </div>
          <h3 class="system-name">Multimodal Remote Sensing Workflows</h3>
          <p class="system-desc">
            RGB imagery, thermal and infrared data, derived spatial layers. Extracting
            actionable signals while controlling uncertainty and false positives.
          </p>
          <div class="system-tech">
            <span class="tech-tag">LiDAR</span>
            <span class="tech-tag">Thermal</span>
            <span class="tech-tag">RGB</span>
            <span class="tech-tag">Multispectral</span>
          </div>
        </div>

        <div class="system-item">
          <div class="system-header">
            <div class="system-status">
              <span class="status-dot"></span>
              <span class="status-text">ACTIVE</span>
            </div>
            <span class="system-tag">QUALITY</span>
          </div>
          <h3 class="system-name">Verification & Quality Control Systems</h3>
          <p class="system-desc">
            Automated and human-in-the-loop layers that surface projection errors,
            alignment drift, and metadata assumptions before downstream impact.
          </p>
          <div class="system-tech">
            <span class="tech-tag">QC Pipelines</span>
            <span class="tech-tag">Monitoring</span>
            <span class="tech-tag">Validation</span>
          </div>
        </div>

        <div class="system-item">
          <div class="system-header">
            <div class="system-status">
              <span class="status-dot"></span>
              <span class="status-text">ACTIVE</span>
            </div>
            <span class="system-tag">DELIVERY</span>
          </div>
          <h3 class="system-name">Secure Data Delivery Architectures</h3>
          <p class="system-desc">
            Multi-tenant access control, lifecycle management, permission boundaries,
            and audit-friendly access patterns with strict tenant isolation.
          </p>
          <div class="system-tech">
            <span class="tech-tag">Auth</span>
            <span class="tech-tag">Multi-Tenant</span>
            <span class="tech-tag">API Gateway</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Research Section -->
    <section class="section" id="research">
      <div class="section-header">
        <span class="section-number">03</span>
        <h2 class="section-title">Research & Production</h2>
      </div>

      <div class="research-content">
        <p class="lead-text">
          My research background in <strong>remote sensing and geospatial analysis</strong>
          directly informs the systems I build today.
        </p>

        <div class="research-grid">
          <div class="research-item">
            <div class="research-icon">📡</div>
            <h4>Spatial Measurements Under Scale</h4>
            <p>How spatial measurements behave under noise, resolution changes, and scale transitions</p>
          </div>
          <div class="research-item">
            <div class="research-icon">🎯</div>
            <h4>Alignment & Downstream Inference</h4>
            <p>How resolution and alignment affect model performance and decision quality</p>
          </div>
          <div class="research-item">
            <div class="research-icon">📈</div>
            <h4>Uncertainty Quantification</h4>
            <p>How to reason about and propagate uncertainty in derived spatial products</p>
          </div>
        </div>

        <div class="cta-box">
          <p>Peer-reviewed publications in journals and conferences</p>
          <a href="https://scholar.google.com/citations?user=TcDAyj0AAAAJ&hl=en" 
             target="_blank" class="link-arrow">
            View Google Scholar
            <svg width="16" height="16" viewBox="0 0 16 16">
              <path d="M3 8h10M8 3l5 5-5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" fill="none"/>
            </svg>
          </a>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section class="section" id="contact">
      <div class="section-header">
        <span class="section-number">04</span>
        <h2 class="section-title">Get in Touch</h2>
      </div>

      <div class="contact-grid">
        <div class="contact-card">
          <div class="contact-icon">✉️</div>
          <h4>Primary</h4>
          <a href="mailto:nan.shang@droneinstitute.ai" class="contact-link">
            nan.shang@droneinstitute.ai
          </a>
        </div>
        <div class="contact-card">
          <div class="contact-icon">📧</div>
          <h4>Personal</h4>
          <a href="mailto:sn1514826@gmail.com" class="contact-link">
            sn1514826@gmail.com
          </a>
        </div>
        <div class="contact-card">
          <div class="contact-icon">💻</div>
          <h4>GitHub</h4>
          <a href="https://github.com/leibowivz" target="_blank" class="contact-link">
            github.com/leibowivz
          </a>
        </div>
      </div>
    </section>

  </main>

  <!-- Footer -->
  <footer class="footer-modern">
    <div class="footer-content">
      <div class="footer-left">
        <div class="footer-brand">
          <span class="brand-bracket">[</span>
          <span class="brand-text">NS</span>
          <span class="brand-bracket">]</span>
        </div>
        <span class="footer-text">Nan Shang, Ph.D.</span>
      </div>
      <div class="footer-right">
        <span class="footer-text">Chief Scientist & Founding Engineer</span>
        <span class="footer-text">Drone Institute</span>
      </div>
    </div>
    <div class="footer-bottom">
      <span class="footer-copy">© 2026 All rights reserved</span>
      <div class="footer-links">
        <a href="https://github.com/leibowivz" target="_blank">GitHub</a>
        <a href="https://scholar.google.com/citations?user=TcDAyj0AAAAJ&hl=en" target="_blank">Scholar</a>
      </div>
    </div>
  </footer>

  <style>
    /* ═══════════════════════════════════════════════════════════════
       MODERN DARK THEME WITH FLUID GRADIENTS
       ═══════════════════════════════════════════════════════════════ */

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --bg-primary: #000000;
      --bg-secondary: #0a0a0a;
      --bg-tertiary: #141414;
      
      --text-primary: #ffffff;
      --text-secondary: #a0a0a0;
      --text-muted: #606060;
      
      --accent-cyan: #00f0ff;
      --accent-purple: #a855f7;
      --accent-pink: #ec4899;
      --accent-orange: #f97316;
      --accent-blue: #3b82f6;
      
      --border: rgba(255, 255, 255, 0.1);
      
      --font-mono: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace;
      --font-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    }

    body {
      background: var(--bg-primary);
      color: var(--text-primary);
      font-family: var(--font-sans);
      line-height: 1.6;
      overflow-x: hidden;
    }

    /* ═══ FLUID GRADIENT BACKGROUND ═══ */
    .fluid-bg {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 0;
      overflow: hidden;
      pointer-events: none;
    }

    .gradient-orb {
      position: absolute;
      border-radius: 50%;
      filter: blur(120px);
      opacity: 0.6;
      animation: float 20s ease-in-out infinite;
    }

    .gradient-orb-1 {
      width: 600px;
      height: 600px;
      background: radial-gradient(circle, var(--accent-cyan) 0%, transparent 70%);
      top: -10%;
      left: -10%;
      animation-delay: 0s;
    }

    .gradient-orb-2 {
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, var(--accent-purple) 0%, transparent 70%);
      top: 40%;
      right: -5%;
      animation-delay: 5s;
    }

    .gradient-orb-3 {
      width: 550px;
      height: 550px;
      background: radial-gradient(circle, var(--accent-pink) 0%, transparent 70%);
      bottom: -10%;
      left: 30%;
      animation-delay: 10s;
    }

    @keyframes float {
      0%, 100% {
        transform: translate(0, 0) scale(1);
      }
      25% {
        transform: translate(50px, -50px) scale(1.1);
      }
      50% {
        transform: translate(-30px, 50px) scale(0.9);
      }
      75% {
        transform: translate(70px, 30px) scale(1.05);
      }
    }

    /* ═══ NOISE OVERLAY ═══ */
    .noise-overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 1;
      opacity: 0.03;
      pointer-events: none;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' /%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' /%3E%3C/svg%3E");
    }

    /* ═══ NAVIGATION ═══ */
    .nav-modern {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      z-index: 100;
      background: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
    }

    .nav-content {
      max-width: 1400px;
      margin: 0 auto;
      padding: 1.5rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .nav-brand {
      font-family: var(--font-mono);
      font-size: 1.25rem;
      font-weight: 700;
      text-decoration: none;
      color: var(--text-primary);
      display: flex;
      align-items: center;
      gap: 0.25rem;
    }

    .brand-bracket {
      color: var(--accent-cyan);
    }

    .nav-links {
      display: flex;
      gap: 2rem;
      align-items: center;
    }

    .nav-links a {
      color: var(--text-secondary);
      text-decoration: none;
      font-size: 0.95rem;
      transition: color 0.2s;
    }

    .nav-links a:hover {
      color: var(--text-primary);
    }

    .nav-cta {
      padding: 0.5rem 1.25rem;
      background: var(--text-primary);
      color: var(--bg-primary) !important;
      border-radius: 6px;
      font-weight: 600;
    }

    .nav-cta:hover {
      background: var(--accent-cyan);
    }

    /* ═══ HERO SECTION ═══ */
    .hero-modern {
      position: relative;
      z-index: 10;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 8rem 2rem 4rem;
    }

    .hero-content {
      max-width: 1200px;
      text-align: center;
    }

    .hero-kicker {
      font-family: var(--font-mono);
      font-size: 0.875rem;
      color: var(--accent-cyan);
      text-transform: uppercase;
      letter-spacing: 0.1em;
      margin-bottom: 2rem;
    }

    .blink {
      animation: blink 1s step-start infinite;
    }

    @keyframes blink {
      50% { opacity: 0; }
    }

    .hero-title {
      font-size: clamp(3rem, 8vw, 6rem);
      font-weight: 900;
      line-height: 1.1;
      margin-bottom: 2rem;
      letter-spacing: -0.02em;
    }

    .title-line {
      display: block;
    }

    .title-gradient {
      background: linear-gradient(135deg, 
        var(--accent-cyan) 0%,
        var(--accent-purple) 50%,
        var(--accent-pink) 100%
      );
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero-desc {
      font-size: 1.25rem;
      line-height: 1.8;
      color: var(--text-secondary);
      max-width: 800px;
      margin: 0 auto 3rem;
    }

    .highlight {
      color: var(--accent-cyan);
      font-weight: 600;
    }

    .hero-meta {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 2rem;
      margin-bottom: 3rem;
      flex-wrap: wrap;
    }

    .meta-item {
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
    }

    .meta-label {
      font-family: var(--font-mono);
      font-size: 0.75rem;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }

    .meta-value {
      font-size: 1rem;
      color: var(--text-primary);
      font-weight: 600;
    }

    .meta-divider {
      width: 1px;
      height: 40px;
      background: var(--border);
    }

    .hero-cta {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    .btn-primary {
      padding: 1rem 2rem;
      background: linear-gradient(135deg, var(--accent-cyan), var(--accent-purple));
      color: var(--text-primary);
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(0, 240, 255, 0.3);
    }

    .btn-secondary {
      padding: 1rem 2rem;
      background: rgba(255, 255, 255, 0.05);
      color: var(--text-primary);
      text-decoration: none;
      border-radius: 8px;
      border: 1px solid var(--border);
      font-weight: 600;
      transition: background 0.2s, border-color 0.2s;
    }

    .btn-secondary:hover {
      background: rgba(255, 255, 255, 0.1);
      border-color: var(--accent-cyan);
    }

    /* ═══ ASCII VISUALIZATION ═══ */
    .ascii-viz {
      margin-top: 6rem;
      display: flex;
      justify-content: center;
    }

    .ascii-scene {
      font-family: var(--font-mono);
      font-size: 0.75rem;
      line-height: 1.4;
      color: var(--accent-cyan);
      background: rgba(0, 240, 255, 0.05);
      border: 1px solid var(--accent-cyan);
      border-radius: 8px;
      padding: 1.5rem;
      overflow-x: auto;
      text-shadow: 0 0 10px rgba(0, 240, 255, 0.5);
    }

    /* ═══ STATS BAR ═══ */
    .stats-bar {
      position: relative;
      z-index: 10;
      max-width: 1200px;
      margin: 0 auto 8rem;
      padding: 2rem;
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid var(--border);
      border-radius: 12px;
      display: flex;
      justify-content: space-around;
      align-items: center;
      backdrop-filter: blur(10px);
    }

    .stat {
      text-align: center;
    }

    .stat-value {
      display: block;
      font-size: 2.5rem;
      font-weight: 900;
      background: linear-gradient(135deg, var(--accent-cyan), var(--accent-purple));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 0.5rem;
    }

    .stat-label {
      font-size: 0.875rem;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .stat-divider {
      width: 1px;
      height: 60px;
      background: var(--border);
    }

    /* ═══ MAIN CONTENT ═══ */
    .main-content {
      position: relative;
      z-index: 10;
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 2rem 8rem;
    }

    .section {
      margin-bottom: 8rem;
    }

    .section-header {
      display: flex;
      align-items: center;
      gap: 1.5rem;
      margin-bottom: 3rem;
    }

    .section-number {
      font-family: var(--font-mono);
      font-size: 3rem;
      font-weight: 900;
      color: var(--accent-cyan);
      opacity: 0.3;
    }

    .section-title {
      font-size: 2.5rem;
      font-weight: 900;
    }

    /* ═══ CARDS GRID ═══ */
    .cards-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
    }

    .card-modern {
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 2rem;
      transition: transform 0.2s, border-color 0.2s;
    }

    .card-modern:hover {
      transform: translateY(-4px);
      border-color: var(--accent-cyan);
    }

    .card-icon {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

    .card-title {
      font-size: 1.25rem;
      font-weight: 700;
      margin-bottom: 1rem;
    }

    .card-desc {
      color: var(--text-secondary);
      line-height: 1.7;
    }

    /* ═══ SYSTEMS LIST ═══ */
    .systems-list {
      display: flex;
      flex-direction: column;
      gap: 2rem;
    }

    .system-item {
      background: rgba(255, 255, 255, 0.02);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 2rem;
      transition: border-color 0.2s;
    }

    .system-item:hover {
      border-color: var(--accent-purple);
    }

    .system-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1rem;
    }

    .system-status {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .status-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: var(--accent-cyan);
      box-shadow: 0 0 10px var(--accent-cyan);
      animation: pulse 2s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }

    .status-text {
      font-family: var(--font-mono);
      font-size: 0.75rem;
      color: var(--accent-cyan);
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }

    .system-tag {
      font-family: var(--font-mono);
      font-size: 0.75rem;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }

    .system-name {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
    }

    .system-desc {
      color: var(--text-secondary);
      line-height: 1.7;
      margin-bottom: 1.5rem;
    }

    .system-tech {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
    }

    .tech-tag {
      padding: 0.5rem 1rem;
      background: rgba(0, 240, 255, 0.1);
      border: 1px solid rgba(0, 240, 255, 0.3);
      border-radius: 6px;
      font-size: 0.75rem;
      color: var(--accent-cyan);
      font-family: var(--font-mono);
    }

    /* ═══ RESEARCH SECTION ═══ */
    .research-content {
      max-width: 900px;
    }

    .lead-text {
      font-size: 1.5rem;
      line-height: 1.7;
      color: var(--text-secondary);
      margin-bottom: 3rem;
    }

    .research-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
      margin-bottom: 3rem;
    }

    .research-item {
      padding: 2rem;
      background: rgba(255, 255, 255, 0.02);
      border: 1px solid var(--border);
      border-radius: 12px;
    }

    .research-icon {
      font-size: 2rem;
      margin-bottom: 1rem;
    }

    .research-item h4 {
      font-size: 1.1rem;
      margin-bottom: 0.75rem;
    }

    .research-item p {
      color: var(--text-secondary);
      font-size: 0.95rem;
      line-height: 1.6;
    }

    .cta-box {
      padding: 2rem;
      background: linear-gradient(135deg, 
        rgba(0, 240, 255, 0.1) 0%,
        rgba(168, 85, 247, 0.1) 100%
      );
      border: 1px solid var(--accent-cyan);
      border-radius: 12px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .link-arrow {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      color: var(--accent-cyan);
      text-decoration: none;
      font-weight: 600;
      transition: gap 0.2s;
    }

    .link-arrow:hover {
      gap: 1rem;
    }

    /* ═══ CONTACT SECTION ═══ */
    .contact-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
    }

    .contact-card {
      padding: 2rem;
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid var(--border);
      border-radius: 12px;
      text-align: center;
      transition: border-color 0.2s;
    }

    .contact-card:hover {
      border-color: var(--accent-purple);
    }

    .contact-icon {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

    .contact-card h4 {
      font-size: 1rem;
      color: var(--text-muted);
      margin-bottom: 1rem;
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }

    .contact-link {
      color: var(--text-primary);
      text-decoration: none;
      font-size: 1.1rem;
      font-weight: 600;
      transition: color 0.2s;
    }

    .contact-link:hover {
      color: var(--accent-cyan);
    }

    /* ═══ FOOTER ═══ */
    .footer-modern {
      position: relative;
      z-index: 10;
      max-width: 1200px;
      margin: 0 auto;
      padding: 3rem 2rem;
      border-top: 1px solid var(--border);
    }

    .footer-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
    }

    .footer-left {
      display: flex;
      align-items: center;
      gap: 1.5rem;
    }

    .footer-brand {
      font-family: var(--font-mono);
      font-size: 1.5rem;
      font-weight: 700;
    }

    .footer-right {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: 0.5rem;
    }

    .footer-text {
      color: var(--text-secondary);
      font-size: 0.95rem;
    }

    .footer-bottom {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-top: 2rem;
      border-top: 1px solid var(--border);
    }

    .footer-copy {
      color: var(--text-muted);
      font-size: 0.875rem;
    }

    .footer-links {
      display: flex;
      gap: 2rem;
    }

    .footer-links a {
      color: var(--text-secondary);
      text-decoration: none;
      font-size: 0.875rem;
      transition: color 0.2s;
    }

    .footer-links a:hover {
      color: var(--accent-cyan);
    }

    /* ═══ RESPONSIVE ═══ */
    @media (max-width: 768px) {
      .hero-title {
        font-size: 2.5rem;
      }

      .hero-desc {
        font-size: 1.1rem;
      }

      .stats-bar {
        flex-direction: column;
        gap: 2rem;
      }

      .stat-divider {
        width: 100px;
        height: 1px;
      }

      .section-number {
        font-size: 2rem;
      }

      .section-title {
        font-size: 1.75rem;
      }

      .nav-links {
        gap: 1rem;
        font-size: 0.875rem;
      }

      .footer-content {
        flex-direction: column;
        gap: 2rem;
        text-align: center;
      }

      .footer-right {
        align-items: center;
      }

      .footer-bottom {
        flex-direction: column;
        gap: 1rem;
      }
    }
  </style>
</Base>
